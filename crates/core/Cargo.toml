[package]
name = "portfoliowebsitebuilder-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Portfolio site bundle load, validate, and render (Phase 1)"

[lib]
path = "src/lib.rs"
